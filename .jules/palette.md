## 2024-05-24 - Add ARIA Labels to Icon-only Buttons
**Learning:** Found multiple instances of icon-only buttons lacking `aria-label` attributes, affecting accessibility for screen readers.
**Action:** Consistently add `aria-label` to any button that only contains icons (like material-symbols-outlined) so its purpose is clear to all users.
