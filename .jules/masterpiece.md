## 2026-09-25 - Cinematic Video Integration
**Learning:** Successfully added a new `CinematicViewer` component that triggers the Replicate `stability-ai/stable-video-diffusion` (SVD) API model to turn a static try-on image into a Hollywood-style motion video. Also learned to use Next.js `next/dynamic` and `framer-motion` effectively.
**Action:** When asked to implement cinematic video generation, use `CinematicViewer` and call the `/api/cinematic-try-on` route which uses SVD.
