## 2026-07-28 - Accessible 3D Overlays
**Learning:** Icon-only buttons used as floating overlays in 3D/Canvas environments often lack accessible names by default since they rely on visual cues (like Material Symbols).
**Action:** Ensure all interactive elements within 3D overlay controls are explicitly labelled with `aria-label` to provide context for screen readers.
