## 2023-10-24 - Accessible Icon Buttons
**Learning:** Icon-only buttons relying on `material-symbols-outlined` need explicit `aria-label` attributes to be announced by screen readers.
**Action:** Always add descriptive `aria-label`s and `focus-visible` outlines to icon-only buttons.
